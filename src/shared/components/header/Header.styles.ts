import { css } from '@emotion/react';

const header = css({
  display: 'flex',
  justifyContent: 'space-between',
  backgroundColor: 'grey',
  minHeight: '80px',
});

const styles = {
  header,
};

export default styles;
