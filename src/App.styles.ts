import { css } from '@emotion/react';

const global = css`
  body {
    margin: 0;
    background-color: #fafafafa;
    font-family: arial;
  }
`;

const styles = {
  global,
};

export default styles;
